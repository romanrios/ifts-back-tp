extends ../layout.pug

block content
    h2.mb-4 Eliminar Cliente
    
    .row.justify-content-center
        .col-md-6
            .alert.alert-danger.mb-4(role="alert")
                i.bi.bi-exclamation-triangle-fill.me-2
                | ¿Está seguro de que desea eliminar permanentemente al cliente #{cliente.nombre} (ID: #{cliente._id})?
                
            .card.shadow-sm
                .card-body
                    h4.card-title Datos del Cliente
                    
                    .list-group.list-group-flush
                        .list-group-item.d-flex.justify-content-between
                            strong Nombre:
                            span= cliente.nombre
                        .list-group-item.d-flex.justify-content-between
                            strong Teléfono:
                            span= cliente.telefono
                        .list-group-item.d-flex.justify-content-between
                            strong Email:
                            span= cliente.email || 'N/A'
                        
                    .d-flex.gap-3.mt-4
                        // Formulario que ejecuta el método DELETE
                        form(action=`/clientes/${cliente._id}?_method=DELETE`, method="POST")
                            button.btn.btn-danger(type="submit")
                                i.bi.bi-trash.me-2
                                | Confirmar Eliminación
                                
                        a.btn.btn-secondary(href="/clientes") 
                            i.bi.bi-arrow-left.me-2
                            | Cancelar y Volver
