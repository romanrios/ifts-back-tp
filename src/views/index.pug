extends layout

block content
  include flashMessages

  if user
    // Contenedor principal relativo
    .position-relative(style="min-height: 350px;")

      h2.mb-4 Bienvenido #{user.username}

      .d-flex.gap-3.mt-4
        a.btn.btn-primary(href="/cuenta") Modificar datos de la cuenta
        a.btn.btn-secondary(href="/auth/logout") Cerrar sesión

      // Imagen posicionada dentro del contenedor
      img.position-absolute(
        src="/burger.png"
        alt="Hamburguesa"
        style="          bottom: 0;
          right: 0;
          width: 380px;
          max-width: 40%;
          opacity: .9;
          pointer-events: none;
        "
      )

  else
    h2.text-center.mt-5 Debes iniciar sesión para acceder a esta página
    a.btn.btn-primary.mt-3(href="/auth/login") Iniciar Sesión
