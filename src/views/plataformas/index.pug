extends ../layout

block content
<<<<<<< HEAD
    // Título con Ícono y clase de margen
    h2.mb-4 🚚 Listado de Plataformas
    
    // Botón principal con Ícono
    a.btn.btn-success.mb-3(href="/plataformas/agregar")
        i.bi.bi-plus-circle.me-2
        | Nueva Plataforma
    
    if plataformas.length
        // Añadimos .table-hover (ilumina filas al pasar el mouse) y .table-sm (tabla más compacta)
        table.table.table-striped.table-hover.table-sm 
            thead
                tr
                    th Nombre
                    th Tipo
                    th Comisión (%)
                    th Creado
                    th Acciones
            tbody
                each plataforma in plataformas
                    tr
                        td= plataforma.nombre
                        td= plataforma.tipo
                        td= plataforma.comisionPorcentaje + '%'
                        td= new Date(plataforma.createdAt).toLocaleDateString()
                        td 
                            // Botón VER/Detalle: Usamos ícono bi-eye
                            a.btn.btn-sm.btn-info.me-2(href=`/plataformas/${plataforma._id}`)
                                i.bi.bi-eye
                            
                            // Botón EDITAR: Usamos ícono bi-pencil
                            a.btn.btn-sm.btn-warning.me-2(href=`/plataformas/${plataforma._id}/editar`) 
                                i.bi.bi-pencil

                            // Botón ELIMINAR/BORRAR: Asumimos que esta acción va en un formulario POST/DELETE
                            // Si usas un formulario, se vería así:
                            form.d-inline(action=`/plataformas/${plataforma._id}?_method=DELETE`, method="POST")
                                button.btn.btn-sm.btn-danger(type="submit")
                                    i.bi.bi-trash
=======
  .d-flex.justify-content-between.align-items-center.mb-3.flex-wrap
    h2.mb-0 Lista de Plataformas
    a.btn.btn-success.mt-2.mt-sm-0(href="/plataformas/agregar") Agregar Plataforma

  .table-responsive
    if plataformas.length
      table.table.table-bordered.table-sm
        thead
          tr
            th Nombre
            th Tipo
            th Comisión (%)
            th.acciones Acciones
        tbody
          each plataforma in plataformas
            tr
              td(style="vertical-align: middle;")= plataforma.nombre
              td(style="vertical-align: middle;")= plataforma.tipo
              td(style="vertical-align: middle;")= plataforma.comisionPorcentaje + '%'
              td.acciones.d-flex.gap-1.align-items-center(style="white-space: nowrap;")
                a.btn.btn-sm.btn-light(href=`/plataformas/${plataforma._id}` title="Ver") 
                  i(class="bi-eye fs-5") 
                a.btn.btn-sm.btn-light(href=`/plataformas/${plataforma._id}/editar` title="Editar") 
                  i(class="bi-pencil fs-5")
                a.btn.btn-sm.btn-light(href=`/plataformas/${plataforma._id}/borrar` title="Borrar") 
                  i(class="bi-trash fs-5")
>>>>>>> 55c5536c504fa26f861259a9cd1f47f73244d357
    else
      p.alert.alert-info.text-center.mt-3 No hay plataformas registradas aún. 
      p.text-center 
        a.btn.btn-success(href="/plataformas/agregar") Agregar una ahora
